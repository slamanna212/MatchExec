apiVersion: v1
kind: Service
metadata:
  name: {{ include "matchexec.fullname" . }}
  labels:
    {{- include "matchexec.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matchexec.selectorLabels" . | nindent 4 }}
